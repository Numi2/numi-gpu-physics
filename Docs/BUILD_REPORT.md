# Build, Metal optimization, and verification report

Date: 2026-07-18

## Delivered implementation

- Swift Package Manager project targeting macOS 14+
- D3Q19 two-relaxation-time lattice Boltzmann fluid solver
- GPU-generated articulated bird body, paired flapping wings, and tail
- Moving-wall bounce-back with previous/current occupancy handling, including link-distance interpolation for the prescribed beta wing
- Momentum-exchange force and torque with deterministic GPU reduction
- Optional six-degree-of-freedom rigid-body update
- Physical/lattice scaling, initial Mach/domain-fit guards, and field readback
- macOS Metal compilation plus live Metal execution regression
- production-Metal periodic shear-wave validation and raw-field archive mode
- production-Metal translating/oscillating planar-wall validation and archive mode
- production-Metal translating-body topology/momentum-closure release gate
- production-Metal fixed-sphere external-flow validation and archive mode
- production-Metal fixed finite-wing external-flow validation and archive mode
- production-Metal published prescribed flapping-wing validation with phase-load and Q/vorticity archives
- physical-domain-preserving `--resolution-scale` control and allocation preflight

## GPU optimization pass

The strict-math execution path now includes:

- direction-major populations and three-dimensional geometry dispatch;
- one GPU preparation thread for timestep-uniform pose and wing kinematics;
- an exact conservative geometry broad phase with a previous-solid exception;
- byte masks that retain both occupancy and body-part IDs `0...4`;
- first-stage force/torque reduction fused into fluid threadgroups, eliminating the full-cell load buffer;
- density/velocity diagnostic stores only on the final visible step;
- one CPU/GPU synchronization after a multi-command-buffer `advance` call;
- no body-integration encoder or dispatch in fixed-bird mode;
- one initial geometry build and initialization of only the population buffer that is first consumed;
- an interior-domain streaming fast path;
- a sphere initializer that writes static geometry, populations, and diagnostic fields in one coalesced pass while retaining the production fluid/load kernels;
- a shared static-canonical sphere/wing orchestration path and a one-pass fixed-wing initializer; and
- uniform skipping of boundary-load arithmetic, the threadgroup barrier, and first-stage reduction on non-final static steady steps, while coupled bird steps retain per-step loads;
- conservative moving-domain load accounting with complete cover/uncover stencil impulse, while retaining explicit legacy force modes for diagnostics;
- one-thread prescribed-wing stroke/pitch preparation plus sphere/slab rejection before beta-planform power evaluation; and
- a compact GPU load-history buffer that records every phase load without per-step CPU synchronization;
- exact prescribed-wing link intersections stored in dormant solid-node population slots, avoiding another full-grid allocation;
- preservation of newly covered density/momentum in the existing macroscopic field slot before those solid-node slots are reused; and
- sparse audit gathering of only boundary-link values instead of the complete `19*N` population lattice.

At the default `96 x 112 x 96` grid, removing the full-cell load buffer and packing masks reduces persistent Metal buffers by `41.34375 MiB`. Fusing the first reduction removes `63 MiB` of global load-record write/read traffic per step. Suppressing internal diagnostic stores removes `19.6875 MiB` per non-captured step, and single-buffer initialization avoids `74.8125 MiB` of startup stores.

The pre-change and optimized strict-math CLI snapshots printed identical body loads and torques at steps 4 and 32. An unarchived local timing snapshot used `/usr/bin/time -p .build/release/birdflow --steps 32 --report-every 32`: it measured `0.28 s` before the pass and optimized samples of `0.19, 0.13, 0.14, 0.14, 0.14 s` (median `0.14 s`). Those raw timing samples were not archived, so this result is indicative only. It includes process startup and runtime shader/library work and is not a reproducible Metal counter study.

## Verification completed on the available host

Host toolchain:

```text
macOS 26.6 (25G5028f)
Apple M4, 10 GPU cores, Metal 4
Apple Swift 6.3
Python 3.10.12
NumPy 2.2.5
Target: arm64-apple-macosx26.0
```

Validation components executed:

```bash
bash -n Scripts/validate.sh Scripts/check-metal.sh
swift test
python3 Scripts/static-audit.py
python3 Reference/shear_wave_reference.py
python3 Reference/shear_wave_convergence.py
Scripts/check-metal.sh
swift run birdflow validate shear-wave --json
swift run birdflow validate moving-wall --json
swift run birdflow validate translating-body --json
swift run birdflow validate sphere --json
.build/release/birdflow validate wing --json
.build/debug/birdflow validate flapping-wing --audit-inputs --chord-cells 16 --json
.build/debug/birdflow validate flapping-wing --compare-link-forces --single-chord-cells 8 --cycles 1 --json
.build/debug/birdflow validate flapping-wing --decompose-link-numerator --single-chord-cells 8 --cycles 1 --json
.build/debug/birdflow validate flapping-wing --momentum-budget --single-chord-cells 8 --cycles 1 --json
python3 Scripts/audit-flapping-coefficients.py /tmp/birdflow-link-force-comparison.json --output ValidationArtifacts/flapping-wing-coefficient-ledger.json
python3 Scripts/audit-dove-moving-wall-link-population-fallback.py
python3 Scripts/audit-dove-moving-wall-distributed-force.py
python3 Scripts/audit-dove-moving-wall-force-covariance.py
python3 Scripts/audit-dove-moving-wall-spatial-interaction.py
.build/debug/birdflow validate flapping-wing --chord-cells 16 --json --archive /tmp/birdflow-flapping-release-20260714
.build/release/birdflow validate flapping-wing --chord-cells 16 --json --archive /tmp/birdflow-flapping-link-release-20260714
.build/release/birdflow validate flapping-wing --chord-cells 16 --json --archive /tmp/birdflow-flapping-promoted-m4-20260714
.build/release/birdflow validate flapping-wing --single-chord-cells 20 --cycles 5 --json --archive /tmp/birdflow-flapping-chord-20-m4-20260714
.build/release/birdflow validate flapping-wing --audit-inputs --single-chord-cells 20 --json
.build/release/birdflow validate flapping-wing --single-chord-cells 24 --cycles 5 --json --archive /tmp/birdflow-flapping-chord-24-m4-20260714
.build/release/birdflow validate flapping-wing --audit-inputs --single-chord-cells 24 --json
python3 Scripts/audit-flapping-refinement.py /tmp/birdflow-flapping-chord-20-m4-20260714/case.json /tmp/birdflow-flapping-chord-24-m4-20260714/case.json /tmp/birdflow-flapping-promoted-m4-20260714/report.json --coarse-audit /tmp/birdflow-flapping-chord-20-input-audit.json --fine-audit /tmp/birdflow-flapping-chord-24-input-audit.json --output ValidationArtifacts/flapping-wing-fixed-thickness-acceptance.json
.build/debug/birdflow replay measured-bird --input Examples/measured-bird-schema-v1.json --audit-only --json
.build/debug/birdflow replay measured-bird --input Examples/measured-bird-schema-v1.json --steps 2 --batch-size 1 --archive /tmp/birdflow-measured-replay-fixture --json
swift test -c release
swift build -c release
```

All listed build, compiler, audit, and test commands passed except the legacy prescribed-wing fluid ladders and the promoted 8/12/16 ladder, which emitted complete reports before their locked nonzero exits. The input preflight originally exposed the halfway-geometry limitation and now passes after the link-distance implementation. The promoted 20/24 fixed-thickness archive composite passes every unchanged scientific gate. The distributed D12/D16 bird-force capture passes source, positivity, momentum, metadata, and independent-audit gates; its marginal result is `mixed-term-distributed-grid-bias`. The archive-only covariance follow-up identifies a `robust-canceling-mean-offset-dominated-pair-covariance` between base reflection and moving wall, while exact spatial allocation classifies that cancellation as distributed and rejects a targeted primitive capture. None of these results authorizes production or D20 promotion.

Results:

- The previously recorded debug/release suite passed, and the added translating-body topology test passed its focused Apple M4 run in `0.20 s` (`0.125 s` after final audit renaming). Together the focused gates lock the static results, published interpolation algebra, analytic kinematics, GPU layouts, CPU/Metal input geometry/link placement, phase capture, vortex diagnostics, component closure, and topology-changing momentum closure.
- Live Metal tests matched moving-wing fixed-body and free-flight multi-command-buffer advances against synchronized one-step advances, including loads, captured fields, and rigid-body state.
- A direct strict-math CPU/Metal rigid-body step matched position, linear/angular velocity, and orientation within `1e-6` under nonzero force and torque.
- Cross-language audit passed for kernel/pipeline names, shared layouts, Swift/Metal D3Q19 direction/weight/opposite tables, and named buffer contracts.
- Apple’s Metal 3.1 offline compiler compiled and linked every kernel with `-Wall -Wextra` and no warning.
- Periodic shear-wave relative mass drift: `1.1879386363489175e-14`.
- Periodic shear-wave relative decay error: `0.002892715023138497` (about `0.289%`).
- Four-grid independent reference convergence order: `1.9861403033324327`.
- Production-Metal finest-grid shear-wave decay error: `0.0028964498775389` (about `0.290%`).
- Production-Metal three-grid convergence order: `1.9870657216321463`.
- Production-Metal maximum actual population-mass drift: `2.9401853382824776e-6` (below the `5e-6` single-precision gate).
- Production-Metal maximum steps 1–8 cell-population difference from the CPU implementation: `1.7881393432617188e-7`.
- Production-Metal batched-versus-stepwise density and velocity differences: exactly zero in the default validation case.
- Production-Metal finest-grid transient Couette profile error: `5.0501359775896124e-5`; isolated top-wall force error: `3.703458540506022e-5`.
- Production-Metal finest-grid oscillating-wall profile error: `0.001796520595387387`; force-phasor error: `0.0011740580418155779`; force phase error: `-0.0011740452422879244 rad`.
- Oscillating moving-wall profile and force convergence orders: `1.986436490328703` and `2.0208052383958144`.
- Maximum moving-wall cross-flow speed across all cases: `1.296122945859679e-6`; dynamic-wall batched-versus-stepwise density, velocity, and selected-wall force differences were exactly zero.
- The moving-wall case exposed and fixed a periodic-edge/solid-corner bug: wrapped links now execute solid bounce-back instead of reading solid populations directly.
- Production-Metal `Re=100` fixed-sphere finest-grid drag coefficient: `1.2170706918439962`, an `11.657861637063863%` difference from the published `Cd=1.09` compact-gate reference.
- Fixed-sphere drag change between the two finest grids: `0.00028850222554029217` (`0.02885%`); finest normalized mirrored-velocity error: `2.6488133134475767e-6`.
- Fixed-sphere side-force and torque leakage passed `1e-3`; batched-versus-stepwise density, velocity, and load differences were exactly zero.
- A rejected 5D cubic trial produced finest `Cd=1.357528228258264` (`24.54%` high). Expanding to a `10D x 6D x 6D` domain reduced the result inside the unchanged `15%` absolute-drag gate, directly demonstrating boundary-proximity contamination rather than hiding it by relaxing acceptance.
- Production-Metal `Re=100`, aspect-ratio-2 fixed-wing finest coefficients were `CL=0.761354209564864` and `CD=0.7071078500190809`, versus approximate published values of `0.75` and `0.75` at `U*t/c=13`.
- Fixed-wing changes from 32 to 40 cells per chord were `1.8772254779546624%` in lift and `0.2286832450047148%` in drag, both below the unchanged `3%` gate. Finest normalized span symmetry was `7.073535397279364e-7`; batch density, velocity, and load differences were exactly zero.
- The accepted `240/320/400` wing ladder took `1569.51 s` with a `9.173911952 GB` peak unified-memory footprint on Apple M4. A separate unoptimized 400-only calibration took `1160.28 s`; because the workloads differ, this supports only the narrow claim that intermediate reduction work was removed without changing coefficients, not a formal speedup percentage.
- The published Li--Nabawy prescribed-wing ladder deliberately failed: 8/12/16-cells-per-chord fifth-cycle means were `(CL, CD) = (7.44525, 9.71093)`, `(8.58575, 9.58558)`, and `(8.60144, 9.67489)` versus `(1.460, 2.046)`. The finest absolute errors were `489.14%` and `372.87%`.
- The independent input fixture cleared the published mapping: gamma-function beta normalization, area, `r1/R`, `r2/R`, integrated stroke/pitch travel, analytic rates, reference area, Reynolds velocity, and coefficient denominator all passed. CPU and production-Metal masks matched cell-for-cell at four phases, with maximum solid wall-velocity disagreement below `8.3e-9`.
- The link-distance fixture now passes: deterministic sparse readback of production GPU link fractions differs from independent CPU surface intersection by less than `0.00071` cell across the 8/12/16 ladder, versus roughly `0.707` cell for fixed halfway placement. The raw phase-`0.25` occupied-volume ratios remain `1.40625`, `1.39815`, and `0.71354` relative to the regularized surface and `3.51563`, `2.33025`, and `0.89193` relative to published thickness; they remain aliasing diagnostics rather than hydrodynamic wall locations.
- The earlier halfway input ladder took `4.65 s`. An initial link-audit traversal that checked directions from every fluid cell took `78.39 s`; enumerating outward from the sparse solid set plus deterministic link sampling reduced the final debug preflight to `7.38 s`. This is about `86x` cheaper than the new `634.61 s` five-cycle ladder while retaining 10,451 independently intersected links across the four phases of all three grids.
- The earlier halfway fluid failure was numerically repeatable rather than noisy: 12-to-16-cell changes were `0.1823%` in lift and `0.9231%` in drag; finest half-stroke symmetry error was `0.2553%`; fourth-to-fifth-cycle difference was `0.1963%`; all five Q/vorticity milestones were finite; and batch density, velocity, and load differences were exactly zero. The finest lift peaks were phases `0.245` and `0.745`. The subsequent link-distance ladder shows that this apparent plateau persists even after accurate sub-cell wall placement.
- The strict-math debug-host calibration and 774 MB archive took `598.07 s` with a `1.045 GB` peak unified-memory footprint on Apple M4. Runtime shader kernels are the same strict-math kernels used by the release command; the timing is recorded as calibration evidence, not a release performance comparison.
- The default fixed-bird and free-flight release executables completed live Metal runs on the M4.
- The one-cycle prescribed-wing smoke case captured 100 phase-load bins and all five finite positive-Q/vorticity milestones. The existing moving-wall gate still passed after switching force evaluation to the local wall frame, and the stationary fixed-wing locked coefficients remained unchanged.
- With link interpolation enabled, a new one-cycle 8-cells-per-chord diagnostic completed in `6.42 s`, produced `(CL, CD)=(7.52808, 9.48408)`, and placed lift peaks at `0.305T` and `0.805T`. It is an execution/timing diagnostic, not a substitute for the five-cycle refinement ladder.
- The completed link-distance five-cycle release ladder produced `(CL, CD)=(7.45076, 9.58556)`, `(8.58688, 9.50008)`, and `(8.60733, 9.61182)`. Compared with halfway, lift moved by only `+0.074%`, `+0.013%`, and `+0.069%`, while drag moved by `-1.291%`, `-0.892%`, and `-0.652%`. Wall-location aliasing is therefore not the dominant coefficient-error source.
- The new two-finest changes were `0.238%` lift and `1.163%` drag; finest symmetry was `0.237%`, fourth-to-fifth-cycle difference was `0.210%`, all vortex phases were finite, and all batch differences were zero. Absolute errors remained `489.54%` lift and `369.79%` drag, and finest peaks remained `0.245T/0.745T`, so the unchanged scientific verdict is failure.
- The link-distance run and 774 MB archive took `634.61 s` with a `1.050 GB` peak memory footprint. This is not a controlled performance comparison with the earlier debug-host run, but the near-identical peak supports the implementation claim that no persistent full-grid link buffer was added.
- The one-cycle 8-cell total/link/topology diagnostic took `9.84 s`. Total closed against independently selected link plus cover/uncover histories within `9.69e-6` lift coefficient, `1.96e-6` drag coefficient, and `1.15e-6` force units. Cover/uncover supplied `0.47%/2.90%` of mean lift/drag and `1.29%/3.01%` of RMS lift/drag; link exchange is the dominant bias path.
- The subsequent one-cycle force-law A/B diagnostic took `12.23 s`. Galilean-invariant momentum exchange produced `(CL, CD)=(7.52805, 9.48616)`; conventional momentum exchange evaluated on the same interpolated populations and combined with the same topology impulse produced `(7.50904, 9.56192)`. The conventional/Galilean ratios were `0.99747` in lift and `1.00799` in drag, while the independent Galilean-invariant component closure stayed below `9.69e-6` coefficient. The wall-frame correction is therefore not the dominant load-error source.
- The CPU-only paper-equation ledger derived `r2/R=0.5593218136`, `U2=0.03500103431`, single-wing area `S=192`, and coefficient denominator `0.11760695065887139`. Every captured raw lift force inferred the same denominator to binary-double precision; recomputed lift differed by at most `5.33e-15`. Mean drag reprojection from bin-averaged vectors differed by at most `4.36e-4`. Matching the published Galilean-invariant lift and drag would require incompatible denominators, `0.6064052` and `0.5453031`. Coefficient normalization is cleared without another fluid run, leaving the shared link numerator as the next fault domain.
- The six-history link-numerator diagnostic took `18.23 s`. Mean base-reflection `(CL, CD)` was `(-6.75135, -62.26542)`, moving-wall population correction was `(14.71945, 72.26319)`, interpolation residual was `(-0.49416, -0.71106)`, and Galilean wall-frame correction was `(0.01902, -0.07576)`. Both conventional and Galilean phase closures stayed below `1.4e-5` coefficient. The moving-wall population term dominates the net load through cancellation against reflected populations; interpolation and Galilean corrections are small.
- The independent near-wing momentum diagnostic measured raw storage-plus-flux mean `(CL, CD)=(1.18092, 2.04933)` and optional equilibrium-reservoir-adjusted `(1.15774, 2.07231)`, while conventional and Galilean accumulation reported `(7.50904, 9.56192)` and `(7.52805, 9.48616)`. The conservative moving-domain estimator produced `(1.18061, 2.04933)`, closing the raw budget within maximum phase residuals `0.002511/0.000247` under a `0.005` tolerance. Its mean correction relative to legacy conventional total is `(-6.32843, -7.51260)`. The fixed-mask link equation is conservative; incomplete uncover stencil accounting was the localized fault domain.
- The new `24^3`, 40-step translating-sphere gate crossed exactly two lattice cells in `0.65 s`, recording 64 covered and 64 uncovered events over 16 transition steps with no solid link on the control surface. Conservative force closed the raw fluid budget at `3.6449e-5` RMS and `8.3824e-5` maximum residual, versus `0.80263` legacy RMS (`22020.8x` improvement). The focused Swift test took `0.20 s`; the existing Couette/Stokes moving-wall test passed after promotion in `1.81 s`.
- Mode six is now the non-diagnostic production default and the normal prescribed-wing default. A short 8-cell, one-cycle production-path run completed in `3.28 s` at `(CL, CD)=(1.18057, 2.04910)`. The previous full flapping ladder is pre-promotion evidence; the promoted five-cycle result is recorded below.
- The promoted five-cycle 8/12/16 ladder completed in `317.89 s` with a `1.051 GB` peak memory footprint and `774 MB` archive. Mean `(CL, CD)` values were `(1.10193, 2.15741)`, `(1.37756, 2.22153)`, and `(1.42346, 2.11525)`. Finest absolute errors were `2.503%/3.384%`; peak timing, midstroke lift, symmetry, periodicity, vortex coverage, batch invariance, and `3.225%` lift convergence all passed. Drag changed `5.024%` between 12 and 16 cells per chord, missing the unchanged `5%` gate by `0.024` percentage points, so the scientific verdict remains failure without threshold relaxation.
- The targeted five-cycle 20-cell case completed in `582.12 s` with a `1.717 GB` peak memory footprint and `977 MiB` archive. It produced `(CL, CD)=(1.48928, 2.16937)`; the 16-to-20 changes are `4.420%` lift and `2.495%` drag, both below the unchanged `5%` criterion. All individual timing, periodicity, symmetry, midstroke, vortex, and mean-load gates pass. The separate input audit also passes with exact mask agreement and sub-`0.00071`-cell wall placement. Because single-grid mode assigns no verdict and 20 cells is the first grid at nominal `0.05c` thickness, it established the fixed-thickness comparison boundary cleared by the following 24-cell result.
- The five-cycle 24-cell completion case took `1393.77 s`, peaked at `2.947 GB`, and wrote a `1.648 GiB` archive. It produced `(CL, CD)=(1.51819, 2.10509)`, within `3.986%/2.888%` of the published means. Fixed-thickness 20-to-24 changes are `1.904%` lift and `3.054%` drag. The independent archive audit reconstructs the production limits, incorporates both input audits and the zero-difference batch report, and passes every gate. This accepts the prescribed flapping-wing canonical without weakening the original thresholds.
- Measured-bird schema 1 now performs a sub-second CPU preflight of provenance, SI units, COM/principal-axis registration, periodic left/right stroke/deviation/pitch/twist pose and rates, domain/thickness resolution, and conservative Mach before Metal allocation. The Mach guard analytically checks extrema between Hermite keyframes rather than only sampled rates. The synthetic conformance fixture planned a `120 x 138 x 120` grid, `3810` steps/cycle, and maximum estimated Mach `0.14670132`; its final timed two-step release production-Metal replay and atomic SHA-linked archive completed in `0.09 s` wall time (`0.0358 s` solver interval). Exact archived-input bytes, SHA-256, and two phase rows were independently checked. The compact record is `ValidationArtifacts/measured-bird-replay-summary.json`. This proves ingestion/replay plumbing only, not measured-bird aerodynamics.
- The Deetjen et al. Ringneck-dove source is qualified as the next prescribed-motion experimental-force benchmark. Remote Zip64 indexing selected `2018_12_11_OB_F03`, reducing the required transfer from `19,294,077,798` compressed bytes to `671,462,764` including its complete processed surface. The standard-library extractor verified all nine member CRCs. Independent MATLAB inspection recovered a `200 x 200` body grid, sparse `381 x 436` left-wing grid, 144 tail meshes, 144 kinematic frames at 1000 Hz, and a 287-sample synchronized force window at 2000 Hz with `8.89e-15 s` maximum nearest-sample residual. Only processed `FxWings/FzWings` are experimental force targets; the mirrored wing, lateral force, per-wing split, and 20-point wing mass distribution remain assumptions or models. No fluid result is claimed until coordinate, topology, wall-velocity, repeatability, and refinement gates pass.
- The Deetjen surface conversion gate now commits a 144-frame, 2,157-vertex, 3,968-triangle fixed complete-surface sequence in 3.73 MB of float32 positions plus 23.8 KB of uint16 topology. Independent CPU decoding reproduces all source/binary identities, component index ranges, nondegenerate triangles, area and bound closure, and adjacent-frame wall speed. Worst absolute area errors are `4.703%` body, `8.905%` wing, and `0.566%` tail. A naive sparse-outline remesh was rejected after exposing a false `91.9 m/s` tip speed; the promoted material-coordinate regularization reaches `25.2305 m/s`, `1.1807x` the deposit's filtered blade-element maximum under a locked `1.25x` ceiling. This clears CPU input parity, not Metal replay or force validation.
- The dedicated indexed-surface Metal gate now replays all 144 Deetjen frames plus five fractional-time probes in `7.02 s` on Apple M4 using generic prepare, raster, and resolve kernels without allocating fluid populations or dispatching collision/force work. Host-side binary interval selection eliminates a redundant timestamp scan in every vertex thread without expanding the 48-byte parameter block. The `59 x 53 x 50` audit retains body, both wings, and tail at every frame and probe. Five independent CPU raster milestones have exactly zero occupancy mismatch, `2.182e-5` maximum occupied-cell wall-velocity difference, and `1.574e-5` maximum signed-distance difference; prepared positions and physical velocities differ by at most `1.669e-8 m` and `1.907e-6 m/s`. This accepts indexed Metal geometry only; aerodynamic force agreement remains open.
- The promoted indexed-surface production integration gate takes `0.24 s` for eight Apple M4 steps. Periodic boundaries and zero sponge isolate the surface while the accepted geometry drives the production interpolated link builder, conservative moving-domain force mode 6, and `stepFluidTRT`. It crosses 39 cells into solid and 53 back to fluid, records 101,262 persistent link events, preserves all four component identifiers, and matches the independent topology-event count exactly. Direct before/after population momentum closes against aerodynamic impulse at `1.789e-5` relative RMS with `3.8846e-8 kg m/s` maximum residual under the unchanged `0.005` gate. The separately reported halfway persistent-link split is diagnostic only. This accepts geometry-to-fluid impulse accounting, not developed flow or experimental force agreement.
- The Deetjen force-registration gate locks two deposited MATLAB scripts in addition to the measured arrays. Their independent equations map stored platform reaction to BirdFlow external force as `[-FxWings, unavailable, -FzWings]`; lateral force is not zero-filled. Nearest lookup and exact camera-zero arithmetic both select indices `191878...192164`, all 144 stored surface times match exactly after normalization, and the 143 intervening samples are fixed at half-frame interpolation points. The 287-sample target spans `0.143 s`, carries `0.0207113 N s` forward and `0.162774 N s` upward impulse, and reproduces the authors' derived per-wing vertical force with zero maximum residual. This accepts input registration for a coarse pilot, not CFD agreement, uncertainty, or refinement.
- The preregistered Deetjen viscosity-floor pilot uses 16 fluid steps per 2 kHz force sample, 800 pre-roll steps, and the authors' 187-sample `0.025...0.118 s` analysis window. A `0.01 m` grid cannot represent the source viscosity inside the Float TRT margin (`tau+=0.50001469`), so the engineering pilot declares `tau+=0.501`, a `68.07x` viscosity distortion, and disables experimental-agreement acceptance. On Apple M4 it localizes the first sampled negative population at step 176 (`5.5 ms`), D3Q19 direction 7, cell `[31,35,29]`, `0.0764` cells from the moving surface; the load becomes nonfinite at step 331, before pre-roll completes. The independent archive audit passes and the integration gate fails. This rejects the current limited-TRT coarse path before any measured-force ladder and directs a fixed-input near-wall collision-operator A/B.
- The fixed-input Deetjen collision screen checks every step of the 800-step pre-roll and reuses the fused load reduction's limiter counters. Production TRT reproduces the defect more precisely at step 150 (`4.6875 ms`) in the same direction-7 near-surface cell. Positivity-preserving regularized BGK and recursive-regularized BGK both complete 800 steps with positive populations and finite loads. Their correction activates in only 55 and 28 cell-steps, respectively—`2.013e-7` and `1.025e-7` of all cell-steps—under the locked `5%` ceiling. The independent artifact audit passes. Both candidates advance only to candidate-specific momentum closure and an extended pilot; neither is promoted to measured-bird production or experimental comparison.
- The candidate-specific Deetjen momentum gate then replays both survivors through all 800 fixed pre-roll steps. A `[7,68) x [7,62) x [7,59)` control volume is five cells outside the complete swept surface, outside the six-cell sponge, and has zero solid-crossing links. Regularized BGK and RR3 close the conservative load against independent momentum storage plus surface flux at `7.944e-4` and `7.987e-4` relative RMS. A separately reduced whole-domain fluid/source ledger closes at `1.1459e-3` and `1.1453e-3`; all four values clear the unchanged `0.005` gate. The audit independently reconstructs every one of the 1,600 samples. Both candidates advance to the extended pilot; neither is selected or compared quantitatively with experiment.
- The fixed Deetjen extended pilot carries both momentum-closed candidates through all 3,776 fluid steps and 187 registered force samples on Apple M4. Regularized BGK and RR3 finish in `13.31 s` and `13.63 s`, remain finite and positive with minima `2.642e-9` and `3.202e-9`, and activate correction only 55 and 28 times (`4.265e-8` and `2.171e-8` of cell-steps). Their endpoint and interval-mean force histories differ by only `0.656%` and `0.882%` normalized RMS. The audit independently reconstructs every registered sample, mean, impulse, peak time, normalized error, activation fraction, and pairwise difference. Measured-force errors remain descriptive because the coarse condition is `68.07x` over-viscous; this gate supports a controlled 8/12 collision discriminator, not experimental agreement or production-operator selection.
- The preregistered Deetjen collision-grid workflow holds `0.08 m` reference length, `0.0075 m` half-thickness, `0.12 m` padding, `0.06 m` sponge, physical timing, `0.136564` maximum Mach, and the `68.07195x` viscosity floor fixed. Both regularized BGK and RR3 complete D=8 and D=12 with positive finite populations; their grid-trend scores are `0.125454/0.125081`, while operator disagreement decreases from `0.8824%` to `0.8164%`. The locked stationary-wall correction result therefore selects RR3 with zero trend penalty and authorizes exactly one D=16 run. That run stops at step `751/7,552` on a negative direction-0 population at `[64,63,68]`, `0.2151` cells from the surface, with finite loads and only `1.435e-7` correction activation. The independent audit passes all 18 workflow checks, verifies no regularized-BGK D=16 allocation, and retains unavailable fine-grid force metrics as absent. This rejects D=16 completion and experimental comparison without weakening any gate.
- The D=16 sparse population provenance replays steps `747...751` at the retained failure cell without modifying production state; its stage prediction matches every actual direction-0 write exactly. At step 751, direction 0 is positive through reconstruction, but moving-boundary directions `2,8,12,13,16` are already negative. The reconstructed speed reaches `1.007461` lattice units (Mach `1.744974`), beyond the rest-equilibrium positivity limit `0.816497`; RR3's scale becomes zero and collision writes the negative equilibrium `-0.003425966`. Persistent topology, no target-direction boundary source, no far field, and zero sponge exclude the other fused writers. The independent CPU audit reconstructs the 19-population moments, RR3 projection, limiter, and output and passes all 13 checks. This localizes the failure but does not yet assign the upstream boundary error among reflection, wall correction, and interpolation residual.
- The D=16 boundary-term discriminator captures all 17 moving-boundary directions at steps 750/751 without mutating production, matching the prior reconstruction within `1.892e-10` and closing reflected + auxiliary + wall terms within `1.747e-10`. Negative directions change from `2,3,10` to `2,8,12,13,16`. At failure every reflected population and auxiliary contribution is nonnegative, while all five wall corrections are negative and dominant. Four links already use halfway fallback; moving-wall halfway fixes none, removing the auxiliary term fixes none, and zero-wall counterfactuals make all five positive. The independent 12-check audit identifies moving-wall-correction admissibility as the first repair surface without promoting zero wall or changing production physics.
- The archive-only D=16 moving-wall A/B reconstructs all 19 pre-step populations and clears candidate A (pre-step local-density normalization) for a controlled ledger: minimum population `5.580e-5`, lattice Mach `0.5482`, no positivity intervention. Candidate B needs an active `0.11505` global positivity scale; neither candidate is enabled in production.
- The opt-in candidate-A D=16 ledger completed the retained 751-step failure horizon in `22.81 s` on Apple M4 with minimum population `1.634e-8`. Near-wing and global force/momentum residuals are `4.719e-4` and `5.306e-4` relative RMS under the unchanged `0.005` gate; the control volume has zero solid crossings, the candidate adds no wall limiter, and the independent nine-check archive audit passes. Production defaults remain unchanged; only the full registered D=16 candidate run is authorized next.
- The most recent complete local release suite passes all 108 tests in `1091.214 s` (`1092.68 s` command wall time); focused archive, candidate-ledger, and full registered-window Metal tests also pass. For the later spatial slice, the release build passes, both new archive/reconstruction tests pass in `0.254 s`, the retained D16 archive test passes in `0.090 s`, and `Scripts/static-audit.py`, both Metal compiler checks, JSON validation, the independent spatial audit, and `git diff --check` are green. The complete suite was not rerun because it deliberately executes another 7,552-step D16 replay even though this preregistered workflow reuses the hashed endpoint. No GitHub Actions were used.
- The full candidate-A D=16 registered window completes all 7,552 steps in `293.34 s` with minimum population `1.025e-8`, all 187 force samples, and near-wing/global relative RMS ledger residuals `6.247e-4/8.312e-4`. The independent 11-check audit reconstructs every ledger vector and registered force bin. Descriptive force error remains `2.1731` normalized RMS at the fixed `68.07x` viscosity floor, so this passes numerical survival/accounting only and authorizes a candidate-specific spatial discriminator—not production or experimental agreement.
- The preregistered candidate-A spatial discriminator reuses that exact D=16 archive and adds only full-window D=8/D=12 runs (`27.07 s`/`106.09 s`). Both cases pass positivity, all 187 registered bins, dual momentum ledgers, and correction-intrusion gates. Force-history change improves monotonically from `12.705%` to `6.268%`, and the fine-pair mean/impulse differences are `1.058%`; nevertheless, the locked `5%` force-history gate fails. The independent audit reconstructs both new per-step ledgers, 374 force bins, all source hashes, trend arithmetic, and the honest rejection. Production and experimental promotion remain blocked.
- The archive-only D12/D16 phase localization rejects D=20 without running new fluid steps. The force difference is mixed (27/187 bins for half-energy; `16.81%` maximum 5 ms window) and strongly rough (`1.351` normalized first difference; `50.27%` upper-half non-DC spectral energy), not smooth distributed truncation. Topology explains only `12.62%`, while near/global residual differences are `0.375%/0.749%` of force-difference RMS, excluding a dominant topology spike or ledger-accounting error under the frozen rules. The independent 11-check audit authenticates every bin and the rejected allocation decision; a lag/band archive discriminator must precede D=20.
- The source-locked archive-only lag/band discriminator also rejects an easy reinterpretation of the `6.268%` miss. The best lag is `-0.02` bin (`-10 us`) and improves five-fold held-out comparison by only `1.506%`, excluding registration as the dominant cause. A 200 Hz nonperiodic DCT low-pass reaches `4.253%` difference only while retaining `74.27%` of combined force energy, failing the frozen `99%` retention rule; the discarded signal cannot be called harmless estimator noise. Neither broadband noise nor coherent low-band bias is established, classification remains `mixed-unresolved`, and D20 stays blocked. The independent audit reconstructs all five folds, five bands, 187 filtered vectors, source identities, and the unchanged rejection with all 11 checks passing.
- The preregistered fixed-geometry temporal-sampling canonical completes D12/D16 in `2.84 s`/`7.89 s` (`10.82 s` wall). At source phase `26.5 ms`, fixed geometry and wall velocity produce exactly zero topology correction; near-wing/global ledgers stay below `0.036%`, and direct-versus-binned impulse identity closes to `8.02e-17`. Endpoint, sample-trapezoidal, and direct-impulse D12/D16 histories differ by `19.587%`, `9.760%`, and `9.487%`: aggregation removes `51.56%` of endpoint disagreement but cannot clear the unchanged `5%` limit. Total eight-bin impulse differs only `0.864%`, so the locked verdict is `mixed-unresolved`, not convergence. The independent 13-check audit reconstructs all 448 per-step forces, 16 bins, three quadratures, both ledgers, source identities, classification, and D20 rejection.
- The locked same-phase duration extension completes 576 D12 and 768 D16 steps in `8.45 s`/`24.04 s` and reproduces the original eight-bin prefix exactly. Impulse-history differences remain `9.487%/9.929%/9.961%` across 8/16/24-bin prefixes; block differences are `9.487%/28.208%/12.379%`, making the late block `30.48%` worse than the first. Although 24-bin cumulative impulse differs by `4.716%`, its force history still fails 5%. Both cases remain positive, topology-free, and ledger-closed below `0.069%`. The preregistered classification is `persistent-fixed-wall-grid-disagreement`, so startup relaxation is rejected and D20 remains blocked. The independent 13-check audit reconstructs all 1,344 substeps, 48 bins, nested windows, exact prefix reproduction, and the allocation decision.
- The frozen-phase geometry-only link audit completes D12/D16 in `1.93 s`/`3.06 s` with no populations or fluid steps. Metal and CPU have zero occupancy mismatches, exact link counts, and at most `0.1815%` force-relevant aggregate difference. D12-to-D16 total/component link measure changes only `1.362%/2.301%`; total/component 20-bin interpolation variation is `3.143%/6.898%`; grid mean velocity and RMS speed change only `0.418%/0.271%`. Area and interpolation clear. The left-wing deposited mean velocity remains `10.742%` from independent thickened-triangle quadrature on D12 and `10.379%` on D16, above the frozen `10%` limit, so the classification is `wall-velocity-deposition-bias` and D20 remains blocked. The 13-check Python audit independently reconstructs all direction/component bins, binary-surface triangle/cap quadrature, metrics, classification, and source hashes. Contract revision 2 records that its unarchived `1e-5` pointwise draft was corrected to the already established geometry-parity envelope while adding a `0.5%` complete aggregate gate; science thresholds did not move.
- The geometry-only wall-velocity A/B then completes D12/D16 in `7.56 s`/`13.66 s`, reproduces the hashed production direction/component moments exactly, and compares them with endpoint interpolation and exact same-component triangle-barycentric velocity at every reconstructed link intersection. Exact sampling does not improve the worst mean error (`10.742%` production versus `10.783%` exact), while endpoint interpolation worsens it to `11.430%`; solid-node velocity sampling is therefore rejected as the cause. The intersection points pass the `0.10`-cell RMS offset-surface gate at `0.0696` cell, but sparse residuals reach `0.8742` cell and fail the frozen `0.75`-cell maximum. The locked classification is `signed-distance-intersection-placement-bias`; the independent 13-check audit passes, and no velocity repair, fluid run, D20 allocation, or production change is authorized.
- The preregistered link-intersection localizer scans all `25,262` D12 and `45,514` D16 links in `0.152 s`/`0.249 s` with no populations or loads and reproduces both source link lists and maximum residuals exactly. Only `8/7` links exceed `0.75` cell, representing `0.0251%/0.0122%` of link measure. None touches a true mesh boundary; `7/8` and `7/7` lie within `0.25` cell of another component's physical offset surface, clearing the frozen `80%` combined association rule at a minimum `87.5%`. Dominant directions differ and contain only `25.0%/28.6%`, rejecting stencil-direction association. The locked `mesh-edge-or-component-junction-associated` classification is therefore descriptively a component-junction association. The independent 13-check audit decodes and reconstructs the binary mesh, nearest triangles, boundary incidence, alternate components, link records, summaries, hashes, and safety boundary. Only an archive-only exact offset-surface ray-root A/B on these 15 links advances.
- The archive-only exact ray-root A/B completes D12/D16 in `0.043 s`/`0.029 s`. All 15 solid/fluid endpoint pairs select different nearest components, and every fluid endpoint selects the recorded alternate component, proving that production linear `q` blends two different surface-distance functions on this set. Exact global-union junction roots still shift `0.5194/0.9435` cell RMS and as much as `0.8860/1.1359` cells, failing the unchanged `0.10/0.75` RMS/maximum gates. D12 owner-to-union RMS improvement is only `38.42%`; D16 is zero because every exact union root remains on the owner component. Roots close below `6.985e-7` cell, and the independent NumPy implementation reconstructs all roots, endpoint identities, metrics, hashes, classification, and safety fields with 13 checks passing. The locked result is `junction-global-root-linearization-bias`; coefficient-sensitivity analysis on these sparse links must precede any boundary or fluid experiment.
- The preregistered coefficient reconstruction evaluates the production five-term interpolated-bounce-back operator on the same 15 links in under `0.04 ms`, with no geometry search or populations. Exact global-union `q` changes the `q=0.5` stencil branch on `3/8` D12 links and all `7/7` D16 links. The measure-weighted operator-coefficient L1 change is `1.723/2.782`, maxima are `2.824/3.189`, the largest individual coefficient change is `0.9515`, and the maximum symmetric operator-norm ratio is `1.294`; these fail the frozen zero-switch, `0.10` RMS, `0.25` maximum, and `1.10x` insensitivity envelope. The independent 12-check audit reconstructs every coefficient, summary, hash, classification, and safety field. The locked result is `branch-changing-coefficient-sensitive`, authorizing only a frozen D12 production-primitive capture and offline population/momentum replay—not a production correction, D20, or full fluid A/B.
- The fallback-aware realized-population replay captures all eight D12 outliers at every one of the 576 fixed-phase steps (`4,608` link-step records) in `10.02 s`. The retained first attempt closed population algebra to `3.32e-9` but exposed four production halfway fallbacks and invalidated its unavailable-farther-node counterfactual. Contract revision 2 preserves the original `10%` local and `1%` global thresholds, records four production and one exact-global fallback, and reproduces effective production `q` within `2.03e-9` with zero metadata mismatches. The corrected exact-`q` response is only `1.822%` population RMS, `1.094%` outlier-force RMS, `0.1085%` of global force RMS, and `0.4428%` of global impulse. Minimum population is `0.00753`, limiter activation is zero, and both momentum ledgers close below `5.94e-4`. An independent Python implementation reconstructs all populations, forces, torques, 576 step sums, impulses, hashes, revision provenance, and safety fields with all 12 checks passing. The locked result is `realized-population-insensitive`: no sparse-link boundary A/B, D16 replay, D20 allocation, or production change is authorized. The next allocation is a distributed full-link component/direction/`q`-bin force-term decomposition.
- The equation-only Deetjen source-scaling audit verifies the deposited `MuscleModel.m` constants `rho=1.18 kg/m^3` and `mu=1.849e-5 Pa s` against its locked SHA-256 and reconstructs the D8/D12/D16 `68.07195x` viscosity floor. The paper publishes neither a Reynolds number nor same-flight ambient temperature, pressure, or humidity. The registered maximum-wall-speed/`0.08 m` source-property proxy is `Re=128,813`; the deposited blade-element-speed/selected-bird mean-chord proxy is `Re=102,417`, a `25.773%` definition difference. D20 cannot satisfy the unchanged `tau+>=0.50005` margin; D28 is the first integer fixed-Courant grid that can. The independent source/MATLAB/equation implementation passes all ten checks. No fluid or Metal work was executed and no production or experimental claim was promoted.
- The diagnostic-only D16 source-viscosity A/B preserves the public `tau+>=0.50005` guard and freezes a package-only `0.50002` execution floor. Both regularized BGK and RR3 completed 1,600 steps at `tau+=0.50002939`, with strictly positive populations, worst momentum residual below `0.069%`, and correction activation below `1.1e-8` of cell-steps. The independent 15-check audit reproduces all per-step and summary arithmetic.
- The D16 evidence rule selected RR3 for the first normal-margin D28 grid. The `259 x 238 x 229` Apple-M4 case used a conservative `3.61 GB` working-set estimate and completed its 2,800-step pre-roll in `740.56 s` at `tau+=0.50005144`. The separately preregistered full window then completed all 13,216 steps and 187 bins in `2,706.01 s`. Minimum population was `4.838e-9`; near-wing/global momentum residuals were `0.0824%/0.1507%`; correction activation was `0.00136%`. The independent 17-check audit reconstructs every step and force bin. Numerical gates pass, but joint normalized RMS force error remains `2.1357`; exploratory decomposition finds correlated but `39.0%` high vertical mean force and weakly correlated, `74.5%` low horizontal mean force. The D28 history advances only as the coarse member of a same-physics D28/D32 refinement pair; experimental agreement, grid convergence, production promotion, and free flight remain open.
- The same-physics RR3 D32 allocation was separately preregistered before each result. Its `296 x 271 x 261` grid contains `20,936,376` cells with a conservative `5.36 GB` working set and `tau+=0.50005877`. The 3,200-step pre-roll completed in `923.59 s`; its independent audit passed all 18 checks. The 15,104-step full window then completed in `4,627.86 s`, retained a positive `4.685e-9` minimum population, closed near-wing/global ledgers at `0.1613%/0.0964%`, activated correction in `0.00144%` of cell-steps, and recorded all 187 force bins. Its independent audit passed all 17 checks. D32 is numerically accepted only as the fine refinement input.
- The D28/D32 refinement comparison was frozen after the D32 numerical report existed but before the histories were compared. Mean force, impulse, and peak time differ by `0.760%`, `0.726%`, and `0%`, but the primary history differs by `5.632%` against the inherited `5%` fine-pair gate; horizontal/vertical changes are `7.376%/4.661%`. The independent 12-check audit reproduces the failure and rejects D36, grid convergence, experimental agreement, production promotion, and free flight. A zero-fluid phase localization then finds `42.67%` of total squared difference in `25...36 ms` and `28.94%` of horizontal difference in the `25...30 ms` target. Its independent 13-check audit supports a targeted D28/D32 component replay there because the best simple lag is zero; no fluid or claim boundary was changed.
- The selected-link reflected-population provenance discriminator preserves its V1 insufficient-coverage run as a locked negative control, then changes only observation capacity in V2. D28/D32 complete `3,360/3,840` steps, capture all 11 endpoints with zero append overflow or detail mismatch, reproduce source reflected force at `8.93e-7/9.34e-7` relative RMS, and cover `100.0%/83.45%` of absolute X/Z score against the frozen `50%` gate; the larger D32 case ran in `1,780.93 s`. The midpoint population/composition identity closes at `5.54e-16`; raw Metal float-force consistency is `1.48e-9`; and the independent 16-check audit passes. Near-wall link composition supplies a temporally stable `91.12%` of the absolute ledger, population history supplies `0.216%`, and their cancelling interaction supplies `8.659%`. This localizes the next experiment to zero-fluid, frozen-population coefficient cross-application and does not authorize D36 or a production boundary edit.
- The preregistered conditioned link-composition discriminator reuses those captured endpoint strata and executes no fluid steps. It reconstructs all 64 D28/D32 hybrid states and applies exact six-factor Shapley attribution. Endpoint reconstruction, source-composition reproduction, Shapley force closure, energy closure, and conditional normalization are all below `5.64e-16`; maximum pooled fallback mass is `0.0743%` against `5%`. Direction composition supplies a stable `87.66%` of the absolute factor ledger in both temporal halves. Link-measure scale is `11.71%` with a cancelling sign; occupancy, q bin, interpolation branch, and topology are each below `0.34%`. The independent 18-check implementation reconstructs the full result and authorizes only one fixed-factor oblique planar/slab canonical—not D36, a production edit, convergence, or experimental agreement.
- The authorized planar direction-composition canonical now runs 40 static cases over four X/Z orientations, `48/64` cells per fixed patch, five subcell phases, and both equilibrium and Deetjen-source midpoint populations. Metal/CPU per-direction counts match exactly. The largest fine analytic-vector error is `1.28364%`, coarse/fine phase-mean change `2.86748%`, fine phase spread `1.18702%`, histogram total variation `0.65695%`, equilibrium normal error `0.47711%`, and tangential leakage `0.33192%`; all eight frozen `5%` gates pass. V1 remains archived as a Float tie-classification negative control, V2 changes only centered-cell evaluation arithmetic, and an independent 14-check NumPy audit reconstructs every count, response, summary, hash, gate, and claim boundary. This clears basic planar D3Q19 weighting and redirects the next zero-fluid discriminator to a source-locked curved surface; it does not authorize a production edit, D36, convergence, or experimental agreement.
- The source-locked curved direction-composition canonical reuses the audited complete-dove link-count archive at sample 53 (`26.5 ms`) and D12/D16; no fluid or new Metal work is executed. Whole-surface opposite counts and the equilibrium response cancel exactly. D12-to-D16 whole histogram TV is `0.130179%`, maximum whole fixed-profile response change `0.009126%`, and maximum component histogram/response changes are `0.707305%/0.711128%`; all seven preregistered gates pass. A separate NumPy implementation reconstructs every component and whole-bird count, histogram, response, source identity, gate, classification, and safety field with `14/14` checks passing. This clears coarse curved redistribution only. Wall velocity, interpolation, D28/D32 direction support, grid convergence, experimental agreement, production promotion, and free flight remain open; the next allocation is a counts-only D28/D32 same-phase capture.
- The source-locked D28/D32 complete-link census executes the sample-53 (`26.5 ms`) geometry through production Metal and independent CPU rasters without populations, collision, streaming, force evaluation, or a new physics kernel. The cases complete in `0.1638/0.2846 s` (`0.4833 s` total; `1.01 s` command wall time) with about `1.89 GB` peak footprint. Both masks and all 144 component/direction counts match exactly. Static link totals `141,018/184,542` differ from the archived moving-run active-link totals by only `0.7481%/0.6351%` against the frozen `5%` gate. D28-to-D32 whole histogram TV is `0.0656866%`, maximum whole fixed-profile response change `0.00116085%`, and maximum component histogram/response changes are `0.248144%/0.194463%`; all eight gates pass. The independent NumPy implementation reconstructs all sources, counts, totals, responses, gates, and boundaries with `16/16` checks passing. This clears fine-pair direction redistribution at one phase only; the next allocation is the same counts-only census over samples `50...60`, not D36 or a production edit.
- The phase-window extension covers all 11 source samples from `25...30 ms` at D28/D32 in `4.063 s` internally (`4.52 s` command wall time). Exact-parity V1 is retained after stopping on four one-cell discrepancies: three `<=4.9e-6`-cell solid/fluid sign ties and one `7.7e-6`-cell component-ownership tie. Arithmetic-only V2 preregisters a `1e-5`-cell, one-tie-per-case qualification while preserving exact whole-surface direction counts and unchanged physical gates. All 22 cases qualify; every one of eight gates passes at all 11 phases. Maximum whole/component histogram changes are `0.078334%/0.625075%`; maximum whole/component fixed-profile response changes are `0.0032435%/0.566462%`. The independent 18-check implementation reconstructs the V1 failure, tie qualification, every phase metric, all hashes, gates, and safety boundaries. This clears static fine-pair direction support across the localized interval only; moving-wall velocity, interpolation, realized populations, force convergence, D36, experimental agreement, and production promotion remain open.
- The README showcase renders the source-locked Deetjen dove directly through the native Metal viewer at `1120 x 630`, 72 unique frames, and 24 fps. It advances monotonically through source samples 27 through 121 (`27...121 ms`, a `94 ms` interval), in a subtly orbiting body-following frame; a visibly labeled, velocity-matched `14 ms` Hermite transition closes the presentation loop without reversing the wingbeat or claiming periodic measured data. The chart remains locked to the audited 15,104-step D32 RR3 full window and its amber `25...30 ms` phase-localization band. The V14 top status plus `PHASE OK` node require the complete selected-link/Shapley/planar/curved/fine chain, the retained exact-parity phase-window failure, the arithmetic-only V2 tie contract, all 22 qualified cases, eight passing gates at every phase, and the independent 18-check audit. The panel reports the worst phase-window values, `0.078%` whole direction-histogram variation and `0.0032%` maximum whole-response change, while `WALL OPEN` and the failed `5.632% > 5%` D28/D32 boundary prevent a force-bearing or convergence claim. Capture rejects a wrong input artifact, hash, claim boundary, size, frame count, rate, file budget, or nonidentical 73rd-frame endpoint probe. The V14 encoding is `9,354,306` bytes with SHA-256 `9cb8afc8a5f1e661ed1ad4d959049454570b584246c4d2811921e48075425c7c`; its encoded seam is `0.700x` the median adjacent-frame difference. The exact V13 binary (`a4d4c88d41fe0d4c9ad80650db5b42077af3729cda15b827a1fa4f82817ba00e`), V12, V11, V10, V9, and earlier heroes are preserved under `Docs/Media/Progress`.
- The locked 1,000-step D=16 recursive-regularization A/B took `132.60 s` on Apple M4. Against the rejected second-order regularized control, the candidate cut activation from `0.02803%` to `0.00645%`, relative L1 correction from `0.05304%` to `0.01932%`, and relative L2 correction from `1.09683%` to `0.35279%`. Positivity, source ledger, force budget, radial closure, and both unchanged correction gates pass. This qualifies the operator only for the D=8/12/16 geometric ladder; it is not yet enabled in flapping or measured-bird replay.
- The locked RR3 D=8/12/16 refinement ladder completed in `35.23 s` on Apple M4. Every case passed positivity, source-ledger, force-budget, and non-intrusive correction gates; activation and L1/L2 correction all decreased with refinement. Promotion is blocked because drag coefficients `1.32042/0.93800/1.04777` are non-monotonic, D12-to-D16 changes `10.476%` against the unchanged `5%` gate, and no Richardson fit exists. Fourth-to-fifth convective-window means still change `11.54%/13.28%/0.052%`, motivating a cheap coarse-grid duration extension before D=20.
- The controlled RR3 D=8/12 ten-convective-time extension completed in `19.78 s` on Apple M4. Both cases retained every positivity, source-ledger, force-budget, control-isolation, and non-intrusive correction gate. D=12 became duration-stable: ninth-to-tenth drag changed `4.543%` and fifth-to-tenth changed `2.177%`. D=8 remained unresolved at `46.848%` and `29.219%`, respectively, with a large ninth-window excursion. The next allocation is therefore a D=8-only shedding-period and block-uncertainty diagnostic, not D=20.
- The preregistered Formation Flight collision/dissipation screen adds a diagnostic-only regularized collision selector while leaving production formation flight on TRT. A fused reduction observes the minimum of every D3Q19 population on every step and counts correction activation without a second population pass. The c8 RR3 smoke completed in `22.23 s`; the five-cycle c16 RR3 case completed in `1075.62 s`. Both stayed positive (`0.01508`/`0.01560`) with zero correction activation. c16 force/torque closure was `1.10e-6`/`3.70e-6`. RR3 nevertheless increased the locked c16-to-c20 wake residual by `28.98%` and coefficient-history residual by `84.82%`. The independent audit passed `51/51`; the sequential rule correctly prevented a c20 RR3 allocation and retained TRT as the production default. A fresh c8 TRT replay reproduced its locked history exactly after instrumentation, proving the disabled branch is observational.
- The follow-on streamwise attenuation localizer executed no CFD. Across five locked phases and three one-chord bands, downstream c16-to-c20 TRT residual density was only `0.818x` the upstream density, below the frozen `1.15` source-dominated threshold. Its independent audit passed `94/94`. This moves the next experiment from bulk collision/transport to phase- and direction-resolved near-wing boundary population generation. It does not authorize quantitative formation benefit or a global c24 ladder.
- The preregistered formation boundary-source census now resolves both owners, two locked phases, and every D3Q19 direction from the exact production pre-step populations. A retained c8 negative control exposed an empty phase-zero host-report slot after exact history replay; the locked narrow amendment preceded both discriminating grids. Corrected c8 closes population reconstruction at `1.24e-7`. Five-cycle c16/c20 replays complete in `890.86/2293.58 s`, reproduce locked histories exactly, close source reconstruction at `9.28e-8/9.09e-8`, and retain force/torque closure below `1.14e-6/4.42e-6`. The primary exact product identity assigns `98.25%` of c16-to-c20 source change to grid-normalized directional link sampling and `1.75%` to conditional population amplitude; all four owner/phase probes agree, and the independent audit passes `319/319`. An immediate archive-only identity splits the parent sampling term into `47.55%` areal link density and `52.45%` D3Q19 direction redistribution, below the frozen `60%` dominance threshold on both pathways; its audit passes `521/521`. The next allocation is one geometry-only c18 bridge retaining both pathways, not a bulk collision/force-law edit, stopped c20 minimum, or global c24 ladder. Quantitative formation benefit remains unauthorized.
- The preregistered geometry-only c18 bridge adds a dedicated no-fluid executable that reuses the production prescribed-wing preparation and union voxelizer, then reads back only the owner mask for CPU D3Q19 counting. It reproduces all archived c16/c20 owner-direction counts exactly, while c16/c18/c20 poses take `0.072/0.090/0.118 s` on Apple M4 with zero overlaps. Primary-leader areal density is `33.7266/33.1111/33.2650`; c18 lies outside the endpoint interval. The frozen result is `latticePhaseAliasingSuspected`, with normalized density/direction/joint-profile midpoint curvatures `0.833/0.503/0.768`; the independent audit passes `105/105`. This selects a geometry-only subcell-offset ensemble before any c18 fluid census or production edit. No GitHub Actions were added or run.
- The selected subcell uncertainty experiment executes the complete `4 × 4 × 4` global-translation tensor at c16/c18/c20: 192 production-Metal voxelizations and owner-resolved D3Q19 counts with zero fluid timesteps. Zero-offset cases exactly replay the bridge; all cases have positive support and zero overlap. Recorded pose/count work totals `3.7104 s`. Ensemble-mean density/direction/joint-profile curvatures fall to `0.1315/0.0926/0.1315`, producing the preregistered `aliasingAveragedOut` result; the independent audit passes `334/334`. This clears smooth ensemble-mean geometry refinement, not force convergence, and selects a matched-median-offset phase-locked source census next.
- The matched median-phase source census selects common offset `[0.25,0.25,0.75]` before translated CFD (`0.82576` normalized-median score versus `1.64086` at zero), then executes coupled-only five-cycle c16/c18/c20 production-TRT cases in `1016.72/1367.68/2544.79 s` on Apple M4. All three pass finiteness, zero-overlap, exact two-owner support, source reconstruction, branch closure, owner force/torque closure, and periodicity; worst residuals are `8.06e-8` source, `7.47e-7` force, and `3.19e-6` torque. Under h-linear interpolation, scalar geometry curvature is `0.14998`, but direction-resolved areal links, conditional population, and exact population-weighted source are `0.78497/0.58733/0.88415`, yielding `mixedPopulationWeightedSource`. Reflected/interpolation/wall curvatures are `0.92415/0.99675/0.56016`. The independent audit correctly rejected a first decomposed-sum analysis `62/64`; that artifact is preserved exactly, a narrow post-run correction enforces the preregistered production source without changing classification, and the corrected audit passes `66/66`. Only the two next-best deterministic offsets are authorized before a power ladder.
- The Formation Observatory V6 presentation uses independently phased copies of the locked Deetjen OB F03 measured-derived complete dove sequence: `2,157` vertices and `3,968` triangles per flyer, source frames `27...121`, and a velocity-matched `14 ms` Hermite presentation closure. Evidence remains explicit: body and left wing derive from measured surfaces, the right wing is a bilateral-reflection assumption, and the reconstructed tail uses audited `[14,6,6]` presentation scaling versus `[16,16,7]` for body/wings. V5 exposed a second presentation defect: the 21 archived CFD slices faded out between captured windows. V6 instead holds the nearest real c20 slice at full opacity through every `48/48` phase, prints its source phase, and performs no field interpolation. The doves and guides never replace prescribed-wing CFD, loads, or power. The 48-frame `1120 × 630` forward-only GIF is `8,144,140` bytes with SHA-256 `54255ff84b855f2124ec0d6fbff2449bab740c6d9f61cef70c1ba89ea5298b61`; encoded seam RMS is `1.005x` the median adjacent change and the V6 audit passes `46/46`. The exact windowed V5 binary is retained under `Docs/Media/Progress`; the mixed source result and `10.68% > 5%` force stop remain unchanged.
- Formation Observatory V7 is the cinematic evidence view. It removes every HUD element, tightens the camera, and adds a three-ridge wake bridge whose path and opacity come from the archived c20 vorticity/vertical-velocity fields, whose cyan-to-violet gradient encodes wake age, and whose luminance follows the complete passed 4,820-step c18 leader-q5 reflected-population trace. Cyclic linear interpolation between adjacent members of the 21-state c20 archive eliminates presentation stepping while remaining excluded from forces, power, and validation. The subtle follower-plane ring is also presentation-only. The 48-frame `1120 × 630` forward-only GIF is `7,638,548` bytes with SHA-256 `1d0dc0835512739e54e6f67352a76ed7de960ef913d38350e3744619d8800e09`; the source endpoint is pixel-identical, encoded seam is `0.931x` the median adjacent change, and the expanded V7 audit passes `55/55`. The exact V6 HUD binary is retained under `Docs/Media/Progress`; the mixed source result and `10.68% > 5%` force stop remain unchanged.
- Formation Observatory V8 replaces V7's restrained drift with a mechanically tested spherical figure-eight camera. Yaw follows `-1.02 + 0.34 sin(2πφ)`, pitch follows `0.38 + 0.10 sin(4πφ)`, and distance follows `10.75 + 0.10 cos(2πφ)`, producing two viewing lobes and multiple upper/lower side-quarter views while returning exactly to frame-zero parameters. Both doves remain framed at all eight inspected phases. The 48-frame `1120 × 630` forward-only GIF is `7,756,091` bytes with SHA-256 `f4af3b62318d0fffd1d2e41fa157cf12e3a054400ba7ba6d4e9973448bde3564`; the source endpoint is pixel-identical, encoded seam is `0.943x` the median adjacent change, and the V8 audit passes `56/56`. The exact V7 binary is retained under `Docs/Media/Progress`; camera motion is presentation-only and all scientific boundaries remain unchanged.
- Formation Observatory V9 removes the diagonal dark beam exposed by the moving camera. Layer-isolation renders proved it was entirely inside the archived field plane, not the q5 wake bridge or wingtip guides. The display now applies a mask-aware radius-4, sigma-2 Gaussian filter, fills the hidden canonical solid gap from surrounding archived fluid samples, and combines vorticity with absolute vertical velocity for opacity. The original fields and owner mask remain byte-identical, and all filtering is presentation-only. Eight-angle inspection confirms the seam is gone. The 48-frame `1120 × 630` forward-only GIF is `7,581,213` bytes with SHA-256 `b17a669ee923ad17281316577c28c704b4ae27d86f912d59b5ec29f533cbb65e`; its source endpoint is pixel-identical, encoded seam is `0.960x` the median adjacent change, and the V9 audit passes `57/57`. The exact V8 binary is retained under `Docs/Media/Progress`; scientific boundaries remain unchanged.
- Formation Observatory V10 visualizes the implemented lattice-Boltzmann method directly. A presentation-only lens is anchored to a real positive-x c20 wake ridge and reproduces the exact D3Q19 topology: one rest/collision node, six axial populations, twelve face-diagonal populations, no cube-corner populations, and outward streaming packets on all 18 moving links. The gold positive-z q5 cue is modulated by the complete passed 4,820-sample leader boundary-source trace. Rendering now uses an `RGBA16Float` scene, a half-resolution 25-tap selective bloom with bounded highlight rolloff, and a single degenerate triangle-strip batch for all wake ribbons; the bloom kernel uses 69% fewer samples than the rejected 81-tap prototype. Multi-angle review caught and corrected a displaced surface-buffer binding that briefly produced wireframe streaks; the independent `64/64` audit now rejects abnormal high-edge-density bursts. The 48-frame `1120 × 630` forward-only GIF is `8,024,801` bytes with SHA-256 `e64059a079e2f6c51cfd9f5e288b9e219895ab446a51d51b668693ceb7e1d064`; its endpoint probe is pixel-identical, encoded seam is `0.979x` the median adjacent change, and maximum high-edge density is `1.182x` the median. Exact V9 is retained under `Docs/Media/Progress`; scientific boundaries remain unchanged.
- The macOS GitHub Actions workflow was deleted. Swift, reference, static-audit, and Metal validation remain explicit local commands and no push or pull request consumes hosted macOS CI minutes.
- Schema 2 now rejects free flight without bilateral measured wing mass,
  hinge-relative COM, and inertia. Its GPU prescribed-rigid-wing momentum
  model feeds left/right inertial hinge reactions into six-DOF dynamics and
  archives them. Independent `1/2/4` body-substep and five-cycle `8/12/16`
  measured-bird load ladders are exposed. A per-step GPU ledger freezes the
  first Mach, sponge/three-cell-clearance, or non-finite failure. The source
  audit still finds no complete same-specimen dataset, so no quantitative bird
  result is claimed.
- The opt-in coupled free-flight ledger now closes direct population momentum,
  whole-bird translation, prescribed-wing internal momentum, far-field,
  sponge, gravity, persistent-link exchange, and the inferred topology
  remainder. Its four-step moving-topology/gravity gate measured `2.4483e-6`
  boundary and `5.0808e-5` total-system relative RMS closure against a locked
  `0.005` limit. Lazy compact reductions leave normal batched production and
  viewer execution unchanged.
- The latest complete local Apple-M4 release suite passed all `125` tests in
  `945.672 s` (`946.19 s` command wall time). The first post-viewer integration
  run correctly rejected an outdated README-capture test fixture after the
  capture became fail-closed over five additional provenance artifacts; that
  fixture was updated, its focused test passed in `0.408 s`, and the complete
  suite was restarted from the corrected bundle and passed. The source-viscosity production-guard and D16/D28/D32 full-window artifact
  reconstruction tests pass alongside the
  new exact-first-event Mach monitor, schema-2 strict loader, CPU/Metal wing
  reaction reference, four-substep parity, and 256-step torque-free and
  constant-torque rotational canonicals all passed, along with the new
  per-part archive, bilateral actuator gate, bounded trim optimizer, and
  byte-exact trim archive round trip, plus the bounded-free-flight dimensionless
  excursion and schema rejection contracts. The release products and both
  standalone Metal libraries compiled; the static audit, 15-check D16 audit,
  17-check D28 audits, 18/17-check D32 pre-roll/full-window audits, 12-check
  refinement audit, 13-check phase-localization audit, 15-check targeted-boundary attribution audit, JSON validation, and byte-stable preregistration
  regeneration also passed locally without GitHub Actions.
- The repository-level `Scripts/validate.sh` boundary also passed locally. Its debug suite completed all `125` tests in `1058.650 s`; the independent shear-wave reference and production Metal paths recovered orders `1.98614` and `1.98707`; moving-wall, translating-topology, fixed-sphere, and fixed-wing canonicals all passed. The fixed-wing 32-to-40-cell changes were `1.877%` lift and `0.229%` drag, with finest `CL=0.761354` and `CD=0.707108`. No hosted CI was used.
- The coupled momentum and per-part canonical also passed a focused
  release-mode run in `0.062 s`; both standalone Metal libraries compiled after the new kernels
  were added, and the static Swift/Metal binding audit passed. No hosted CI
  was used.
- Opt-in GPU part-load reconstruction now attributes the exact production
  conservative exchange to body, left wing, right wing, and tail, then reports
  hinge-shifted aerodynamic torque, prescribed-wing inertial reaction,
  required actuator torque, and signed power. The compact four-step canonical
  closed force/torque sums at `1.0876062e-6`/`3.1473154e-6` relative RMS.
- That diagnostic exposed and fixed the right-wing anatomical pitch/tip-twist
  sign convention. With the physical mirror restored, relative bilateral
  residuals are `7.6318046e-7` force, `4.4050828e-6` hinge torque, and
  `1.5049285e-5` actuator power against the unchanged `0.02` limit.
- A bounded forward-flight trim harness now searches only body-local pitch and
  airspeed, scales Reynolds number with speed to preserve physical viscosity,
  and minimizes all six force/moment components. Two-cycle candidates feed a
  five-cycle selected-point confirmation under unchanged `5%` balance and
  stationarity gates. Archives retain the base and exact derived best input;
  no real-specimen trim result is claimed while that input remains absent.
- The bounded optimizer and byte-exact archive round trip passed in debug and
  release mode (`0.005 s` focused test). The release CLI exposes separate
  screening/confirmation durations and rejects ambiguous duration, free-flight,
  refinement, and momentum-ledger option combinations.
- A combined `--free-flight-confirmation` release gate now restarts the exact
  selected trim input for a minimum five-cycle bounded trajectory, one-cycle
  `1/2/4` body refinement, and one-cycle direct momentum/per-part load closure.
  It archives the byte-identical input, trajectory, runtime safety, and each
  nested report atomically. Fast tests independently verify the dimensionless
  excursion algebra and reject schema-1 input before Metal allocation; no real
  specimen result is claimed.
- The preregistered first alternate Formation source phase freezes the
  deterministic rank-2 offset `[0.5,0.75,0.5]` and changes no production
  physics or threshold. Five-cycle c16/c18/c20 TRT cases complete in
  `397.70/772.67/1114.85 s`; all numerical, overlap, owner, periodicity,
  source-reconstruction, branch, and completeness gates pass. Exact
  population-weighted-source curvature falls from the representative `0.88415`
  to `0.57541`, while the two-offset mean is `0.58368`; both remain above the
  frozen `0.5` smooth boundary. Areal/conditional curvatures are
  `0.61730/0.60159`, component curvatures are `0.54555/0.60586/1.05465`, and
  the independent audit passes `109/109`. The recorded scalar geometry
  curvature `1.17661` prevents a population-only attribution. The locked result
  remains `mixedPopulationWeightedSource`; only the final authorized offset may
  advance, and no formation-power or biological claim is promoted.
- The final Formation source phase freezes rank-3 offset `[0.25,0,0.5]`, the
  exact analysis environment, and a joint promotion rule before CFD: the
  direction-resolved three-offset mean source curvature must be at most `0.5`
  and maximum pairwise phase spread at most `5%`. New c16/c18/c20 cases finish
  in `497.74/891.05/1630.52 s`; every numerical and accounting gate passes.
  Individual source curvatures are `0.88415/0.57541/0.68861`; three-offset mean
  areal/conditional/source curvatures are `0.58081/0.56446/0.59595`. Source
  spread clears at `1.3844%`, but mean curvature fails. Mean component
  curvatures are `0.62088/0.67654/0.86144`, and the independent audit passes
  `190/190`. The locked `mixedPopulationWeightedSourceMean` result blocks the
  wider power scout and authorizes only an archive-only c18 direction/component
  residual-covariance selector before at most one focused phase trace.
- The preregistered archive-only c18 residual selector executes zero fluid
  timesteps and reconstructs the exact h-linear source and additive component
  residuals from all nine locked censuses. Its frozen `10%` concentration and
  two-of-three sign gates select leader reflected momentum exchange at D3Q19
  `q=5`, `[0,0,+1]`, with `21.7875%` of the positive systematic-alignment
  ledger and `3/3` agreement. Reflected `q=6` ranks second at `16.5199%`,
  localizing the residual to a vertical reflected-population pair. The
  representative offset `[0.25,0.25,0.75]` has the strongest selected local
  alignment; residual component closure is `1.37e-8`, and the independent
  audit passes `57/57`. Exactly one c18 final-cycle `q=5` reflected-population
  trace advances; the power map and production edits remain blocked.
- The single authorized c18 temporal trace records all `4,820` final-cycle
  leader-q5 samples in `1323.54 s` through one compact read-only census
  dispatch per step. It reproduces the locked coupled summaries and phase
  anchor exactly, closes source algebra at `2.71e-7`, retains force/torque
  closure at `6.83e-7/3.19e-6`, and passes an independent `59/59` audit. The
  production Metal kernel and GPU layout remain byte-identical to baseline.
  Half of centered source energy needs `0.4844` cycles, while per-link source
  has `|r|=0.4284` near/far occupancy association; the frozen result is
  `cycleDistributedBranchAssociated`. The next allocation is sparse
  matched-phase c16/c20 sampling stratified by branch occupancy, not a narrow
  phase window, second c18 trace, power map, or production edit.

## Verification boundary

The current tests prove buildability, cross-language consistency, the independent reference algebra/convergence result, production-Metal periodic shear-wave decay and convergence, steps 1–8 population agreement, translating and oscillating planar-wall profiles and forces, topology-changing momentum closure, fixed-sphere curved-boundary drag/refinement/symmetry, isolated fixed-wing lift/drag/refinement/symmetry, prescribed-wing analytic normalization/kinematics, CPU/Metal geometry agreement, sub-cell link placement, phase capture/vortex diagnostics, fixed-thickness 20/24 prescribed-wing convergence, measured-data contract/interpolation/GPU replay plumbing, live Metal command ordering, moving-wing/free-flight batch invariance, field capture, deterministic load agreement, and one-step CPU/GPU rigid-body parity. Forced channel flow remains absent. The prior interpolated full flapping ladder used the retired legacy force default; its refinement and repeatability evidence remains diagnostic, but its absolute-load verdict is not a result for the promoted estimator. The prescribed flapping-wing canonical is accepted; quantitative complete-bird use remains blocked by an actual measured specimen, higher-fidelity measured surface geometry where required, executed real-specimen trim and per-part results, bird-load grid convergence, and free-flight momentum/body-step refinement.

Quantitative complete-bird use still requires the remaining ladder in
`Docs/VALIDATION.md`, including forced channel flow, Metal-versus-reference
field comparisons, one real same-specimen dataset and appropriate surface
representation, executed bird-load/body-step refinement, confirmed trim, and
executed real-specimen external-momentum evidence. Runtime Mach/domain
monitoring, rigid prescribed-wing inertial hinge reaction, and the external
linear-momentum archive gate are now implemented. The optimization timings
above are engineering evidence only and are not aerodynamic validation.
